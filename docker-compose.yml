services:
  helloworld:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: HelloWorld
    stdin_open: true
    tty: true
    working_dir: /opt/NAtiveMpyTest
    command: sleep infinity
    volumes:
      - .:/opt/NAtiveMpyTest

